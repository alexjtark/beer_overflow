<div class="container">
       <h2><div id="ask-link"> <%= link_to 'Ask a beer question!', new_question_path %></h2>
       </div>
    <div class="row">
        <% @questions.each do |q| %>
          <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
            <div class="post-preview">
                <h2 class="post-title">
              <%= link_to "#{q.title}", question_path(q.id) %>
                </h2>
                  <p1> <%= q.content %></p1>
                 <p class="post-meta">
                  <%= q.asker.username %>
                  <%= q.date_posted  %>
                  <strong>Votes</strong> <%= q.vote_count%> |
      <%=link_to 'Up', question_up_path(q.id)%>|<%= link_to 'Down', question_down_path(q.id)%>
              </div>
            <hr>
          </div>
         <% end %>
      </div>
    </div>
